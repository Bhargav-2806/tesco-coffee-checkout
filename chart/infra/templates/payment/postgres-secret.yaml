# Postgres credentials
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: payment
type: Opaque
data:
  POSTGRES_USER: {{ .Values.postgres.user | b64enc }}
  POSTGRES_PASSWORD: {{ .Values.postgres.pass | b64enc }}
  POSTGRES_DB: {{ .Values.postgres.db | b64enc }}
