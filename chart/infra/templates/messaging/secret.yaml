# RabbitMQ admin credentials — b64enc runs at install time
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
  namespace: messaging
type: Opaque
data:
  RABBITMQ_DEFAULT_USER: {{ .Values.rabbitmq.user | b64enc }}
  RABBITMQ_DEFAULT_PASS: {{ .Values.rabbitmq.pass | b64enc }}
