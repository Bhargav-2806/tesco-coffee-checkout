# Ingress host comes from values.yaml — easy to override per environment
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tesco-ingress
  namespace: checkout
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: react-nginx
                port:
                  number: 80
