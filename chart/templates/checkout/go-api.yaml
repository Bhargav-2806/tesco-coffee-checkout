# Go API — image now comes from values.yaml
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-api
  namespace: checkout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-api
  template:
    metadata:
      labels:
        app: go-api
    spec:
      containers:
        - name: go-api
          image: {{ .Values.images.goApi.repository }}:{{ .Values.images.goApi.tag }}
          ports:
            - containerPort: 8080
          env:
            # $(VAR) substitution requires the referenced vars to come FIRST
            - name: RABBIT_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-client-credentials
                  key: RABBITMQ_DEFAULT_USER
            - name: RABBIT_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-client-credentials
                  key: RABBITMQ_DEFAULT_PASS
            - name: RABBITMQ_URL
              value: "amqp://$(RABBIT_USER):$(RABBIT_PASS)@rabbitmq.messaging:5672/"
---
apiVersion: v1
kind: Service
metadata:
  name: go-api
  namespace: checkout
spec:
  selector:
    app: go-api
  ports:
    - port: 8080
      targetPort: 8080
